import { supabase } from "../lib/supabaseClient";

export default function Login() {
  return (
    <div style={{ padding: 40, fontFamily: "Arial" }}>
      <h1>Login Page</h1>
      <p>Supabase client connected successfully.</p>
    </div>
  );
}
